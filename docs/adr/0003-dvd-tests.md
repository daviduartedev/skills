# dvd-tests is the delivery gate (Drive, Verify, Deliver)

The published GitHub repo is `daviduartedev/skills`. The delivery skill after implement was `smoke-test-list` (local run + unchecked human checklist). It is now **dvd-tests**: the same human gate, plus a browser Drive against that local URL and printed E2E results. This is a rename and deepening of the existing delivery skill, not a fifth published skill. Scanners and SDD orchestrators stay out.
